[package]
name = "log_puller_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.104"
dashmap = "6.2.1"
libssh-rs = { version = "0.3.8", features = ["vendored", "vendored-openssl"] }
rayon = "1.12.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
